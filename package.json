{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^7.0.1",
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "pub": "lerna bootstrap && lerna publish && git push --tags",
    "repub": "lerna publish from-package"
  }
}
